apiVersion: v1
kind: Service
metadata:
  name: demo-app-stable
  namespace: default
  labels:
    app: demo-app
spec:
  selector:
    app: demo-app
  ports:
    - name: http
      port: 3000
      targetPort: 3000